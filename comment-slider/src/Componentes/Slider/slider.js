import Comentario from "../Comentario/comentario1";

export default function Slider({Comentario}){
    const [comentario, setComentario]= useState(0)
    const cantidad = comentarios?.length;

    return(<></>)
}